import { IconLogo } from "../../icons/IconLogo";
import styles from "./styles.module.css";

export const Logo = () => {
  return (
    <div className={styles.logo}>
      <IconLogo />
    </div>
  );
};
